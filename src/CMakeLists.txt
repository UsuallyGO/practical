
PROJECT(src)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

ADD_SUBDIRECTORY(list)
ADD_SUBDIRECTORY(stack)

INSTALL(
    DIRECTORY ${PROJECT_SOURCE_DIR}/../include/src/
    DESTINATION ${CMAKE_INSTALL_PREFIX}/include/src
    FILES_MATCHING PATTERN "*.h"
)
